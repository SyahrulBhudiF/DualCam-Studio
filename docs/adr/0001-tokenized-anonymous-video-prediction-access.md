# Tokenized Anonymous Video Prediction Access

Prediksi Video can be created without login, but public result access requires both `predictionId` and an access token. This keeps the upload flow low-friction for skripsi/demo usage while avoiding bare-ID public result exposure; admins use separate management routes to list and inspect all Prediksi Video without public tokens.
